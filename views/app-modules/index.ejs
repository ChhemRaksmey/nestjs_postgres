<!-- 
<h2>Application Modules Setup</h2>
<hr>
<div class="d-flex justify-content-between mb-3">
    <form action="/app-modules" method="GET" class="d-flex">
        <input type="text" name="search" class="form-control me-2" placeholder="Search Module..." value="<%= search %>">
        <button type="submit" class="btn btn-outline-secondary">Search</button>
    </form>
    <a href="/app-modules/create" class="btn btn-primary">+ Add New Module</a>
</div>
<table class="table table-bordered table-striped shadow-sm bg-white">
    <thead class="table-dark">
        <tr>
            <th>Module ID</th>
            <th>Full Name</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
        <% if (modules.length > 0) { %>
            <% modules.forEach(mod => { %>
                <tr>
                    <td><%= mod.app_module_id %></td>
                    <td><%= mod.full_name %></td>
                    <td class="text-center">
                        <a href="/app-modules/edit/<%= mod.app_module_id %>" class="btn btn-sm btn-warning">Edit</a>
                        <a href="/app-modules/delete/<%= mod.app_module_id %>" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure you want to delete this module?')">Delete</a>
                    </td>
                </tr>
            <% }) %>
        <% } else { %>
            <tr><td colspan="3" class="text-center">No records found.</td></tr>
        <% } %>
    </tbody>
</table> -->

<div class="row">
    <div class="col-12">
        <div class="page-title-box d-flex align-items-center justify-content-between">
            <h4 class="mb-0">System Application Module</h4>

            <div class="page-title-right">
                <a role="button" class="btn btn-sm w-sm btn-primary" href="/app-modules/create">Add New</a>
                <a role="button" class="btn btn-sm w-sm btn-primary">Search</a>
                <a role="button" class="btn btn-sm w-sm btn-primary">Help</a>
            </div>

        </div>
    </div>
</div>

<div class="row font-size-12">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
        
                <table id="datatable" class="table table-striped table-bordered dt-responsive nowrap">

                    <thead>
                        <tr>
                            <th class="py-1 col-11">Name</th>
                            <th class="py-1 col-1 text-center">Action</th>
                        </tr>
                    </thead>
        
                    <tbody>
                        <% if (modules.length > 0) { %>
                            <% modules.forEach(mod => { %>
                                <tr>
                                    <td class="py-1">Tiger Nixon</td>
                                    <td class="py-1 text-center">
                                        <div class="dropdown dropstart">
                                            <a role="button" class="" id="dropdownMenuButton" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                                <i class="fas fa-tools"></i>
                                            </a>
                                            <div class="dropdown-menu font-size-12" style="inset: unset !important;">
                                                <a class="dropdown-item py-1" href="/app-modules/view/<%= mod.app_module_id %>">View</a>
                                                <a class="dropdown-item py-1" href="/app-modules/edit/<%= mod.app_module_id %>">Edit</a>
                                                <div class="dropdown-divider my-0 border-2"></div>
                                                <a class="dropdown-item py-1" href="#">Authorize</a>
                                                <div class="dropdown-divider my-0 border-2"></div>
                                                <a class="dropdown-item py-1" href="#">Reset</a>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        <% } %>
                    </tbody>

                </table>
        
            </div>
        </div>
    </div>
</div>
